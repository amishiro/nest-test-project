{
  "devDependencies": {
    "@prisma/client": "^3.12.0",
    "@types/node": "^17.0.25",
    "prisma": "^3.12.0",
    "ts-node": "^10.7.0",
    "typescript": "^4.6.3"
  },
  "scripts": {
    "prisma": "prisma",
    "migrate": "prisma migrate dev --name init",
    "studio": "prisma studio",
    "docker-prisma-studio": "npm install && npm run migrate && npm run studio"
  }
}
